{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the app's own window is allowed to call. Tauri v2 denies every core and plugin API unless it is listed here; the app's own #[tauri::command]s do not need an entry.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
